<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="../css/home.css" />

    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    />
    <title>Home</title>
  </head>
  <body class="bg-gray-50 min-h-screen font-sans text-gray-800">
    <%- include('./includes/navbar.ejs'), {isLoggedIn: isLoggedIn}%>

    <div class="container mx-auto py-8 px-4">
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        <% if(projects.length > 0) { %> <% for(let project of projects) {%>
        <div
          class="bg-white shadow-md rounded-lg overflow-hidden transition-transform transform hover:scale-105"
        >
          <img
            src="<%= project.image %>"
            alt="<%= project.image %>"
            class="w-full h-48 object-cover"
          />
          <div class="p-4">
            <h3 class="text-lg font-semibold text-gray-900">
              <%= project.projectTitle %>
            </h3>
            <p class="text-sm text-gray-500 mt-1"><%= project.projectType %></p>
            <p class="mt-2 text-gray-700 text-sm"><%= project.overview %></p>

            <% if (isLoggedIn === "true") { %>
            <div class="flex space-x-2 mt-4">
              <a
                href="/edit-project/<%= project.id %>"
                class="text-indigo-600 hover:text-indigo-400 transition-colors"
              >
                <i class="bi bi-pencil-fill"></i> Edit
              </a>
              <a
                href="/delete-project/<%= project.id %>"
                class="text-red-600 hover:text-red-400 transition-colors"
              >
                <i class="bi bi-trash-fill"></i> Delete
              </a>
            </div>
            <% } %>
          </div>
        </div>
        <% } %> <% } else { %>
        <div class="col-span-full text-center py-20">
          <h1 class="text-2xl font-bold text-gray-600">No Project Available</h1>
          <p class="text-gray-500 mt-2">
            Please check back later or add a new project.
          </p>
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
