<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" />
    <title>Edit Project</title>
</head>
<body class="bg-gray-50 min-h-screen font-sans">
    <%- include('./includes/navbar.ejs'), {isLoggedIn: isLoggedIn} %>

    <div class="flex items-center justify-center mt-8">
        <form class="bg-white shadow-lg rounded-lg p-6 w-1/3" action="/edit-article/<%= project.id %>" method="post" enctype="multipart/form-data">
            <h2 class="text-2xl font-bold text-center mb-6">Edit Project</h2>

            <div class="space-y-4">
                <div class="grid grid-cols-8 gap-4">
                    <div class="col-span-8 md:col-span-6">
                        <label class="block text-lg font-semibold mb-1">Project Title</label>
                        <input type="text" name="projectTitle" value="<%= project.projectTitle %>" class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:border-blue-500" placeholder="Enter project title" required />
                    </div>

                    <div class="col-span-8 md:col-span-2">
                        <label class="block text-lg font-semibold mb-1">Type</label>
                        <select name="projectType" class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:border-blue-500" required>
                            <option value="<%= project.projectType %>"><%= project.projectType %></option>
                            <option value="Web Application">Web Application</option>
                            <option value="Website">Website</option>
                            <option value="Mobile Application">Mobile Application</option>
                            <option value="API">API</option>
                        </select>
                    </div>
                </div>

                <div>
                    <label class="block text-lg font-semibold mb-1">Project Overview</label>
                    <textarea name="overview" class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:border-blue-500" rows="5" placeholder="Write project overview..." required><%= project.overview %></textarea>
                </div>

                <div>
                    <label class="block text-lg font-semibold mb-1">Select Image</label>
                    <input type="file" name="image" class="w-full border border-gray-300 rounded focus:outline-none focus:border-blue-500" />
                </div>
            </div>

            <button class="w-full mt-4 bg-blue-500 text-white py-2 rounded hover:bg-blue-600 transition-colors text-lg font-semibold" type="submit">
                Edit Project
            </button>
        </form>
    </div>
</body>
</html>
